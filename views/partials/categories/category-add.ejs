<div class="offcanvas offcanvas-end offcanvas-large" tabindex="-1" id="addCategory">
    <div class="offcanvas-header border-bottom">
        <div>
            <h5 class="mb-0 fw-bold">Add Category</h5>
            <small class="text-muted">Create a category for your transactions</small>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
    </div>

    <div class="offcanvas-body">
        <form action="/categories/add" method="post">
            <div class="mb-3">
                <label class="form-label fw-medium">Category Name</label>
                <input type="text" name="name" class="form-control" placeholder="Food, Travel, Freelance..." required>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label fw-medium">Type</label>
                    <select class="form-select" name="type" required>
                        <option selected disabled value="">Select Type</option>
                        <option value="income">Income</option>
                        <option value="expense">Expense</option>
                    </select>
                </div>

                <div class="col-md-6 mb-3">
                    <label class="form-label fw-medium">Icon</label>
                    <select class="form-select category-select-compact" name="icon">
                        <option selected disabled value="">Select Icon</option>
                        <% iconOptions.forEach((icon) => { %>
                            <option value="<%= icon.value %>"><%= `${icon.emoji} ${icon.label}` %></option>
                        <% }) %>
                    </select>
                    <div class="category-option-note mt-2">Pick an emoji-style colorful icon for better visual grouping.</div>
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label fw-medium">Note</label>
                <textarea class="form-control" name="description" rows="3"
                    placeholder="Add a short note about this category..."></textarea>
            </div>

            <button type="submit" class="btn btn-primary w-100 py-3 fw-semibold">
                <i class="ti ti-device-floppy me-2"></i>Save Category
            </button>
        </form>
    </div>
</div>
